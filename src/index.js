import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//J'ai pas compris celle la par contre aucune idée ?
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);